<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.QuizApplication" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>


    <style name="AppTheme" parent="Base.Theme.QuizApplication">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/primary_color</item>
        <item name="colorAccent">@color/white</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="colorControlHighlight">@color/gray</item>

    </style>
    <style name="CardViewStyle" parent="Widget.Material3.CardView.Outlined">
        <item name="shapeAppearanceOverlay">@style/CutShape</item>
    </style>

    <style name="CutShape" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerFamily">cut</item>
        <item name="cornerSize">@dimen/_12sdp</item>
    </style>

    <style name="DialogCustomTheme" parent="Theme.Material3.DayNight.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
    </style>

    <style name="myMaterialButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textColor">@color/primary_color</item>
        <item name="strokeWidth">2dp</item>
        <item name="android:background">@color/white</item>
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <!-- Text Styles -->

    <style name="TitleTextViewStyle" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/large_text_size</item>
        <item name="android:textColor">@color/txt_color</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAlignment">center</item>
        <item name="fontFamily">@font/inter_semi_bold</item>
    </style>

    <style name="PrimaryTextStyle" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/medium_text_size</item>
        <item name="android:textColor">@color/primary_text_color</item>
        <item name="fontFamily">@font/inter_semi_bold</item>
    </style>

    <style name="SecondaryTextStyle" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/regular_text_size</item>
        <item name="android:textColor">@color/txt_color</item>
        <item name="fontFamily">@font/inter_semi_bold</item>
    </style>
    <style name="CustomButtonStyle" parent="Theme.AppCompat.Light">
        <item name="android:background">@drawable/common_ripple_effect_btn</item>
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:padding">8dp</item>
        <!-- Add any other properties you want to customize -->
    </style>

</resources>